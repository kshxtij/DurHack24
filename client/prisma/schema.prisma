datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

generator client {
  provider = "prisma-client-js"
}

model Alert {
  id       Int      @id @default(autoincrement())
  appName  String
  contains String
  severity String
  cron     String
}
